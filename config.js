var config = {};
config.facebook = {};

/*
Will probably change this to just app id and secret in 
the future.
To get your app access token, see 
http://developers.facebook.com/docs/opengraph/using-app-tokens/
*/
config.facebook.appId = "YOUR_APP_ID"
config.facebook.appAccessToken = "YOUR_APP_ACCESS_TOKEN"

module.exports = config;